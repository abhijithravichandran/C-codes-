#include<stdio.h>
int main(){
    int a=3.0/9;
    printf("the vale of 3.0/9 is %d",a);
    return 0;
}
// Here the value will be zero because integer declaration,  'a' can not store the floating values